# Auth0 Integration

```
  auth0ClientID=''
  auth0Logout=''
  auth0Domain='z'
  auth0Theme={{
    primaryColor: `#00cfff`,
  }}
  auth0Options={{
    rememberLastLogin: false,
    auth: {
      redirect: false,
    },
  }}
```