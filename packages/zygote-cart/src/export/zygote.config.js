/* eslint-disable */
"use strict";

module.exports = {
	plugins: [
		// require('./plugins/zygote-plugin-standard-billing'), // Turn on the standard billing plugin to replace stripe's input
		// require('@escaladesports/zygote-plugin-esca-api'), // Working example plugin
	],
}